import styled from 'styled-components';

export const ProjectCardElement = styled.li`
  min-width: 300px;
  max-width: 900px;
`;

export const ImageBox = styled.div`
  min-width: 300px;
  max-width: 900px;
`;

export const InformationField = styled.div`
  padding: 40px 0;

  h3 {
    font-weight: 500;
    font-size: 30px;
    line-height: 1.2;
    letter-spacing: 0.02em;
    margin-bottom: 15px;
  }

  p {
    font-weight: 400;
    font-size: 20px;
    line-height: 1.5;
    letter-spacing: 0.02em;
  }
`;
